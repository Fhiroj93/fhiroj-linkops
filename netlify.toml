[build]
  command = "npm run build"
  publish = "dist/client"

# Fallback so client-side routes (e.g. /calendar, /review) don't 404 when
# someone loads them directly instead of navigating there from "/".
# Harmless to keep even if server functions are working correctly.
